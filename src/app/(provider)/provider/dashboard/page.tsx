'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ProviderDashboardPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Provider Dashboard Test</Typography>
      <Typography>If you see this, the dashboard page is loading correctly.</Typography>
    </Box>
  );
} 